# Day 127 Summary — Task Snapshot Creation

Starting an active workflow creates a running `WFTE-...` snapshot for each ordered task. Later task edits cannot rewrite the earlier execution history.

## Next Step

Add controlled task-level outcomes and display.
