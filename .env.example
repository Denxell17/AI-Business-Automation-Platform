# Use sqlite for local development or postgresql for production.
DATABASE_BACKEND=sqlite

# Required only when DATABASE_BACKEND=postgresql.
DATABASE_URL=postgresql://abap_user:change_this_password@localhost:5432/abap